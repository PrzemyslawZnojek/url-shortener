spring:
  datasource:
    url: jdbc:postgresql://dbpostgresql:5432/url-shortener
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: liquibase/changelog.yaml